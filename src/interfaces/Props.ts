import { ReactNode } from "react";


export interface IPropsPersonaProvider {
    children: ReactNode
}

export interface IStyles {
    content: React.CSSProperties,
    btn: React.CSSProperties,
    form: React.CSSProperties,
    input: React.CSSProperties
}